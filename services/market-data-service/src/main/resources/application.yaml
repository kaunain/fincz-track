server:
  port: ${SERVER_PORT:8084}

spring:
  application:
    name: market-data-service
  cache:
    type: simple

# External API configurations
market:
  api:
    base-url: ${MARKET_API_BASE_URL:https://api.example.com}
    key: ${MARKET_API_KEY}

# Cache configuration
cache:
  ttl: ${CACHE_TTL:300000}